import Btn from '../components/Btn';

const UserInfo = () => {
  return (
    <>
      <Btn text={'Sub'} type={'sub'} />
      <Btn text={'Main'} type={'main'} />
    </>
  );
};

export default UserInfo;
