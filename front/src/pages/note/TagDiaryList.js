import { Modal } from '@mui/material';

const TagDiaryList = (props) => {
  const { setOpenTagList } = props;
  return (
    <Modal setOpen={setOpenTagList}>
      <div>sdfdsdfsdfd</div>
    </Modal>
  );
};

export default TagDiaryList;
